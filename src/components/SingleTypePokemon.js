{
  /* {pokemon.types.length === 1 ? (
            <View
              style={[
                styles.cardBodyType,
                {
                  backgroundColor:
                    POKEMON_TYPES_COLORS[pokemon.types[0].type.name],
                },
              ]}
            >
              <Text style={styles.cardBodyTypeText}>
                {pokemon.types[0].type.name}
              </Text>
            </View>
          ) : (
            <View
              style={[
                styles.cardBodyType,
                {
                  backgroundColor:
                    POKEMON_TYPES_COLORS[pokemon.types[0].type.name],
                },
              ]}
            >
              <Text style={styles.cardBodyTypeText}>
                {pokemon.types[0].type.name}
              </Text>
            </View>
          )}
          {pokemon.types.length === 2 ? (
            <View
              style={[
                styles.cardBodyType,
                {
                  backgroundColor:
                    POKEMON_TYPES_COLORS[pokemon.types[1].type.name],
                  top: 20,
                },
              ]}
            >
              <Text style={styles.cardBodyTypeText}>
                {pokemon.types[1].type.name}
              </Text>
            </View>
          ) : null} */
}
